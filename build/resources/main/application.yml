
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hipple
    username: root  # MySQL ????
    password: 1234 # MySQL ????
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # update, validate, create, none (?? ??? ?? ??)
    show-sql: true  # SQL ?? ?? ?? (??? ??)
    properties:
      hibernate:
        format_sql: true  # SQL ?? ?? ?? ??
  mysql:
    datasource:
      url: jdbc:mysql://localhost:3306/hipple
      username: root
      password: 1234
  data:
    username: root
    password: 1234
    driverClassName: com.mysql.cj.jdbc.Driver
